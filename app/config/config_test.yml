imports:
    - { resource: config_dev.yml }

parameters:
    database_host: m2gl.deptinfo-st.univ-fcomte.fr
    database_name: m2test6

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

swiftmailer:
    disable_delivery: true
